# Voice

- Web Speech is the default voice engine.
- Vosk is the offline fallback.
- Commands stay short: `record`, `pause`, `resume`, `stop`, `play`, `rewind`, `fast forward`, `again`.
- `commands.js` decides whether text becomes a command.

