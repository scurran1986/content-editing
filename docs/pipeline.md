# Pipeline

Local pipeline:

```bash
npm run pipeline
```

It runs compile checks, unit tests, SAST, Docker boot, and browser tests.

Set `RUN_VOSK=1` to fetch the offline model and run the Vosk test too.

